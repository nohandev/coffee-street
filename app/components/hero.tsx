import Navbar from '@/app/components/ui/navbar'

export default function Hero() {
  return (
    <main className='bg-hero h-screen flex items-center justify-center'>
      <Navbar/>     
    </main>
  )
}
